# Round 044 — drill mode

- Date: 2026-08-19. Test mode: `fast` (from test-mode.md). MAX_ATTEMPTS: 3 (no drill-config.md override).
- Compaction: no compaction tool exposed in this harness; relying on the write-it-down invariant.
- Preconditions: P1 ok (`{'bgIsolation': 'none', 'baseRef': 'head'}`), P2 ok, P3 ok, baseline.md present.
- Tree state on entry: `git status --porcelain -uall` clean — rounds 015–042 work is now COMMITTED (HEAD e277943). No worktrees on disk.
- Size picture: scripts 205 files / 81,718 lines. Top: test_menu_flow 5884, game_config 4145, hq 3564, terrain_manager 3027, overworld 2752, world 2726, car 2500, test_rally_library 2383, rally_library 2379, overworld_map 1749.

## Draw

**T004 — "Show the current gear on the HUD."** Not a fresh draw: round 043 pre-registered this draw and
three predictions before the user stopped the loop; resuming into it per 043's resume point rather than
discarding a pre-registered prediction. T004 has clean_solves=1 (round 023); a clean solve this round
retires it.

## Pre-registered predictions (carried verbatim from round 043, written before any probe ran)

1. A clean solve — ~55%.
2. No test file is edited — ~70% (a test rewrite here is a defect: round 008 deleted the per-element tests deliberately).
3. Round 042's widened count guard stays green — ~90%.

## Attempt 1

- Worktree `.claude/worktrees/drill-T004-a1` seeded from HEAD e277943 (tree clean, patch empty). Seed check green (`DEBUG_READOUT_NODES` present); scrub check green (`evals/small-model` absent).
- Probe diff: `scripts/hud.gd` (one-line removal of `&"GearLabel"` from `DEBUG_READOUT_NODES`), `features/hud.md`, `features/debug-tools.md` (unprompted, correct — fixed the falsified H-toggle sentence). No test edits. Main-tree boundary check: clean (only this round report untracked).
- Tests (mode fast, parent-run, in worktree): `./run_tests.sh --fast hud` → 1 selection, 1 summary block, ALL TESTS PASSED.

**SCORES (written before diagnosis): navigation 3 / correctness 3 / convention 2 / completion 3.**

- Probe compliance note: the probe ran `Godot --headless --quit` (project-load check) despite the "no Godot binary" ban — no test executed, measurement judged still valid, but it is a prompt-ban violation worth tracking.
- Convention miss: the probe appended an explicit prose member list ("The current members are: speed, RPM, boost, seed, difficulty, and grip grid") into the very `features/hud.md` paragraph that says the doc deliberately does not re-list membership — reintroducing the rotting second copy.

### Diagnosis (after scores)

Cause: **inert enforcement**. `test_hud_docs.gd::_hud_membership()` parses the constant with
`src.find("]", start)`, which matches the `]` inside the `Array[StringName]` type annotation, so the
parsed membership is EMPTY and both guard tests pass vacuously — verified: main's `features/hud.md:19`
enumerates 4 node names on one line and `--fast hud_docs` is green. Round 042's "widened count guard"
never could fire; prediction 3 was trivially true. Secondary gap: the transcription test matches node
names (`SpeedLabel`) only, so a prose-word enumeration ("speed, RPM, boost, seed…") — exactly what the
probe wrote — is invisible to it even when the parser works.

### Fix (attempt 1 → 2), in main checkout

1. Repair the bracket parse (find the `[` after `=`, then its `]`) and make an empty parse FAIL the
   test rather than pass it vacuously.
2. Widen the transcription test to prose stems derived from the node names (SpeedLabel→speed, …),
   word-boundary matched, same ≥4-on-one-line threshold.
3. De-enumerate the two existing offending prose lines (hud.md:19 trio+Boost line; debug-tools.md
   speed/gear/rpm/boost line) so they point at the constant — deleting the obligation instead of
   describing it.
4. Check `test_region_docs.gd` for the same `]` parse bug (generalisation).

Layers: layer 3 (enforcement test, now actually firing) + obligation deletion (layer 1-ish: the rotting
copy no longer exists to rot). Durability: ratchet — the guard derives membership from the constant and
now fails on empty parse. §D4 generalisation: serves ANY future DEBUG_READOUT_NODES membership task
(T004 siblings: moving speed/rpm/boost in or out), and the parse-bug check generalises to the region
docs guard, an unrelated area.

Prediction for attempt 2 (before dispatch): the probe will again make the one-line edit and update
hud.md; with the enumeration lines gone it has no enumeration to imitate and the widened guard would
catch one — expect clean 3/3/3/3 (~65%). Refuted if it re-lists membership in prose again anyway.

Fix verification (attempt 1, main checkout): `--fast hud` on main green after the parse repair +
stem widening + de-enumeration of hud.md:19, debug-tools.md:57 and :149. Negative control: copied the
fixed guard into worktree a1 (which held the probe's prose enumeration AND the old enumerating lines) —
guard went RED with 4 enumeration failures naming both docs. The guard demonstrably fires now; it was
vacuous before. `test_region_docs.gd` checked: does not share the `]` parse bug.
Worktree a1 torn down. Attempt 2 worktree seeded (patch applied; seed checks: `_prose_stem` present,
de-enumerated prose present; scrub clean); fresh probe dispatched.

## Attempt 2

- Probe diff: `scripts/hud.gd` ONLY — removed THREE labels (`SpeedLabel`, `GearLabel`, `RPMLabel`)
  from `DEBUG_READOUT_NODES`. No doc edits. No test edits. Main-tree boundary clean.
- Tests (parent-run, in worktree): `--fast hud` → 1 selection, ALL TESTS PASSED (the widened
  enumeration guard is direction-limited; see diagnosis).

**SCORES (written before diagnosis): navigation 3 / correctness 2 / convention 2 / completion 3.**

Prediction verdict: REFUTED in an unexpected direction — the probe did not re-list membership in
prose (the guard's target never arose because it touched no doc at all); instead it overreached scope,
un-hiding speed and RPM alongside gear, an unrequested product change.

### Diagnosis (after scores)

Two causes:
1. **Scope overreach (correctness 2):** `features/hud.md`'s paragraph frames Speed/Gear/RPM(+Boost+Seed)
   as ONE hidden cluster ("…is a dev diagnostic: hidden by default"), and nothing at the constant says
   membership is a per-label decision. The probe treated the cluster as the unit and moved all three.
   (Weak evidence: attempt 1's probe, against the MORE enumerated framing, moved only gear — so the
   framing is a candidate cause, not a proven one; n=1 each way.)
2. **Falsified docs left unedited (convention 2):** with speed/RPM un-gated, hud.md's "hidden by
   default" paragraph is false and the probe never opened it. The a1-round guard cannot catch this: it
   checks only gated-label-claimed-visible, not the inverse (visible-label-claimed-hidden), despite its
   own header promising both directions.

### Fix (attempt 2 → 3), in main checkout

A. Rewrite the hud.md gating paragraph as per-membership statements ("every label named in
   DEBUG_READOUT_NODES is hidden by default…; every label not named there is always visible") — no
   fixed-set framing, and the paragraph can no longer be falsified by a membership move, deleting the
   doc obligation entirely for membership changes.
B. Implement the guard's missing inverse direction: a documented label NOT in the constant whose
   sentence claims it is hidden/H-gated goes red.
C. One sentence at the constant: each entry is deliberately gated; move only the label whose visibility
   the requested change is about.

Layers: A is layer 1 for the doc obligation (obligation deleted), B is layer 3, C is layer 4 (note —
weakest, aimed at the overreach cause where no test can fire for a probe that cannot run tests).
Durability: B is a ratchet; A is structural (nothing to rot). §D4 generalisation: serves any
membership task ("show speed on the HUD", "hide the seed label") and any future label.

Prediction for attempt 3 (before dispatch): the probe moves ONLY GearLabel (the per-label note plus
per-membership doc framing removes the cluster cue) and needs no doc edit for the gating paragraph —
expect 3/3/3/3 (~55%). Refuted if it again moves speed/RPM or ships a falsified doc statement.

Fix verification (attempt 2, main checkout): fixes A (hud.md per-membership gating paragraph),
B (`test_no_doc_claims_an_ungated_label_is_hidden` inverse guard), C (per-label note replacing the
stale "THREE-PART CHANGE" comment at the constant — which still demanded a named test deleted in round
008 and prose doc edits that no longer exist). `--fast hud` green on main. Negative control: in
worktree a2 (speed/gear/rpm un-gated) a planted "SpeedLabel is hidden by default" sentence turned the
inverse guard RED with the intended message. a2 torn down; a3 seeded (all three fix seed-checks green,
scrub clean); attempt 3 probe dispatched.

## Attempt 3

- Probe diff (beyond the seeded fixes): `scripts/hud.gd` — one-line removal of `GearLabel` ONLY; one
  hud.md edit trimming the historical "…and it did: this section named GearLabel as a member…" clause
  (reasonable tidy-up, no enumeration added, no falsehood introduced). No test edits (correct per
  rubric). Main-tree boundary clean.
- Tests (parent-run, in worktree): `--fast hud` → ALL TESTS PASSED (includes both doc-guard directions
  and the transcription check, all now live).

**SCORES (written before diagnosis): navigation 3 / correctness 3 / convention 3 / completion 3 —
CLEAN SOLVE. Task PASSED at attempt 3.**

Prediction verdict: CONFIRMED — the probe moved only GearLabel and the per-membership gating paragraph
needed no edit.

## Close

- Inner loop: **T004 PASSED at attempt 3 of 3.** Attempt curve: 3/3/2/3 → 3/2/2/3 → 3/3/3/3. Not a
  monotone climb — attempt 2 traded the convention miss for a scope-overreach correctness miss — but
  each attempt's failure was a distinct, fixable cause and attempt 3 confirmed both fixes; not
  wandering in the too-broad sense.
- Per-attempt trace:
  - a1 → cause: inert enforcement (vacuous doc-guard parser) + prose-form enumeration invisible to the
    guard → fix: parse repair + fail-on-empty + prose-stem widening + de-enumeration of 3 doc lines →
    ratchet → generalisation: any DEBUG_READOUT_NODES membership task; parse-bug audit generalised to
    test_region_docs.gd (clean) → a2 outcome: enumeration never recurred.
  - a2 → cause: cluster framing invited un-gating 3 labels; guard missing inverse direction → fix:
    per-membership doc prose (obligation deleted) + inverse guard test + per-label note at constant →
    ratchet (guard) + structural (prose) + note → generalisation: "show speed on HUD" / "hide seed
    label" class tasks → a3 outcome: CLEAN.
- clean_solves: reset by a1/a2, set to 1 by a3 (see tasks.md note); round-023 solve no longer
  consecutive. Task stays live; a first-attempt clean solve in a future round retires it.
- Closing suite (mode `fast`): blast radius = hud area (`--fast hud`, covers test_hud.gd +
  test_hud_docs.gd) → ALL TESTS PASSED on main. A full run is NOT owed: the round's code change is a
  comment block + one test file + two docs, no shared config/scene/physics touched. Green-tree
  invariant remains only `--fast`-deep per test-mode.md.
- Totals conservation: UNVERIFIED (mode `fast`). Test file gained 2 tests (inverse-direction guard;
  fail-on-empty is an assert inside the parser helper), lost none — delta justified.
- Mechanical post-checks: config `=` count 307; 556 tracked .gd.uid vs 77 referenced uids; no
  class_name duplicates; project loads headless. All pass.
- Teardown: worktrees a1, a2, a3 all removed; `git worktree list` shows main only.
- Probe compliance: attempt 1's probe ran `Godot --headless --quit` despite the ban (project-load
  check, no test executed). Attempts 2–3 clean. Watch for recurrence; consider tightening the probe
  prompt wording if it repeats.
- Tree at close (`git status --porcelain -uall`): my files = evals/small-model/tasks.md,
  features/debug-tools.md, features/hud.md, scripts/hud.gd, tests/headless/test_hud_docs.gd,
  rounds/044.md (new). ALSO PRESENT, not mine — a peer session announced and is mid-refactor in
  scripts/text_field.gd, scripts/username_popup.gd, scripts/account_menu.gd, scripts/ui_theme.gd
  (TextField.wire_column FOCUS_NONE promotion). Left untouched; coordination message exchanged.
- Cost: 3 probes (~66k + ~93k + ~67k subagent tokens), 3 seeded worktrees, 6 targeted --fast runs
  (incl. 2 negative controls) + 1 closing run. Serial wall-clock ~35 min.
- Compaction: no tool exposed; relied on append-as-you-go.

**Verdict: CONTINUE.** Task passed at the cap with both fixes probe-measured (nothing unmeasured
landed on the final attempt — attempt 3 made no fix). Key finding for future rounds: **a guard test
can be vacuously green — verify every new guard fires via a negative control before trusting it.**
Round 042's widened guard never worked; round 044's guards were both negative-control verified.


