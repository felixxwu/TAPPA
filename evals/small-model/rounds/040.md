# Round 040 — 2026-08-19 — drill mode

**Test mode: `fast`.** Totals UNVERIFIED; full run **OWED** (thirty-nine rounds).
**`MAX_ATTEMPTS = 3`.** No compaction tool; scores to disk before diagnosis (§D3a).

## Sampling — T001, where a clean solve would be the loop's FIRST RETIREMENT

**Drawn: T001 — "Add a gravel-spec tyre upgrade to the catalogue."** `clean_solves: 1` (round 021's 3/3/3/3).

§1 retires a task after **two consecutive clean solves in rounds it was sampled in**, resetting to 0 on any non-clean
attempt. T001 has not been sampled since round 021, so a clean solve now is the second consecutive one and **retires it
to `solved.md`** — the first retirement in forty rounds, and the readiness signal §1 says to read instead of the scores.

T004 also sits at `clean_solves: 1` (round 023). T001 is the stronger pick because **its area has changed since**,
which makes the measurement informative either way — see the risk below.

**Noun grep and state verified:** no gravel tyre exists; the tyre-slot procedure has its three numbered parts, round
020's symmetric invariant, round 039's new gate-id rule, and round 017's tyre-doc guard.

## A risk I am recording BEFORE the probe, because it cuts against my own recent fix

**Round 039 added ~9 lines to this exact note** (the gate-id requirement). Round 021's clean solve happened with the
*shorter* version. §2.6 warns explicitly that "notes at the edit site have a ceiling, and you will hit it" and to
"count the comment lines you are adding" — the `REGIONS` table's ~30-line block is the recorded case of a note probes
demonstrably read and still did not fully obey.

So this round doubles as a test of whether my own round-039 addition **diluted** a note that was working. If T001
regresses on a part the note already covered — the invariant, or the docs — the note's length is the first suspect, and
the fix would be to shorten it rather than add again. **Stating that now so a regression cannot be attributed
elsewhere afterwards.**

## PREDICTIONS, before dispatch

1. **A clean solve (3/3/3/3), retiring T001 — held at ~50/50 and no higher.** Correctness has been reliable here since
   round 020's symmetric invariant landed (round 021 authored a whole new axis correctly), but a clean solve needs
   convention 3, which needs **both** docs. The docs record across the loop is 0/2, 1/2, 1/2, 2/2, 2/2, 0/1, 0/2,
   2/3 — roughly a coin flip, and round 032 concluded it is irreducibly noisy.
2. **Round 039's gate-id rule holds** — a real special id, or ungated with a stated reason (which the note permits).
   This is an immediate test of last round's fix, one round after landing it.
3. **The symmetric invariant holds** — every tyre row still wins somewhere.

## Attempt 1 — SCORES (written to disk before any diagnosis, §D3a)

| axis | score |
|---|---|
| navigation | **3** |
| correctness | **3** |
| convention | **3** |
| completion | **3** |

**3/3/3/3 — CLEAN SOLVE. Passed on attempt 1 of 3.** This is T001's **second consecutive clean
solve** in a round it was sampled in, so §1 **RETIRES it** — the first retirement in forty rounds.

### What the probe did, verified against the diff rather than its report

Three files, exactly the expected set, and nothing else in the tree touched:
`scripts/upgrade_library.gd`, `features/upgrade-catalogue.md`, `features/drivetrain-and-tires.md`.

- **Route: the no-new-axis answer**, building the part from the existing flat + tarmac + snow terms.
  The round-009 rubric note records this as "a fully correct alternative answer", so it is not a
  shortcut — the round-004 registry's three edits are not required for a part needing no new axis.
- **The symmetric invariant HOLDS, in both directions.** Effective grip, recomputed from the authored
  rows rather than from the probe's arithmetic: `gravel_tires` 1.20 gravel / 1.08 tarmac / 1.14 snow;
  `race_tires` 1.15 on every surface; `snow_tires` 1.296 snow / 1.08 gravel / 0.918 tarmac. Per
  surface — gravel: 1.20 > 1.15 > 1.08. Tarmac: 1.15 > 1.08 > 0.918. Snow: 1.296 > 1.15 > 1.14.
  **Each of the three rows wins exactly one surface**: (a) the new part wins somewhere, and (b) it
  takes the last surface from nobody. Note how tight (b) is — race_tires keeps tarmac by 0.07 and
  snow by 0.01 over the new part.
- It **reproduced the win-table in its own code comment**, as round 021's probe did.
- **Ungated, with the reason stated in a comment** — which the note explicitly permits ("leaving a
  new one ungated is a choice, so say why in a comment if you make it"). Verified this is coherent
  and not a dead part: `rally_gate_met` returns `true` when no gate is authored, and `free` is a
  separate concept (always-selectable, never mystery-box-drawn), so the part is reachable by ordinary
  purchase without requiring an event win — exactly what the comment claims.
- **Both docs, 2/2** — four edits in `upgrade-catalogue.md` (slot list, the trade-off paragraph, the
  catalogue inventory, the menu-label count from "two" to "three") and one in `drivetrain-and-tires.md`.

### Tests — all five green, run by me in the probe's worktree

`test_upgrade_library`, `test_upgrades_grid`, `test_tire_surface_axes`, `test_rally_library`,
`test_drivetrain` (33/33). Round 017's `test_every_tyre_slot_part_is_documented_in_the_tyre_doc`
passing is the objective confirmation that both docs name the new id.

**A trap I claimed and then RETRACTED.** I first recorded that `./run_tests.sh --fast a b c d e` ran
only the LAST name, on the evidence of a single `Scripts 1` summary reading "Ran Scripts matching
test_drivetrain" — and I was about to "correct" `features/testing.md:406`, which documents multi-name
`--fast` as "one selection pass each". Checking first: a two-name run emits **two** `Ran Scripts
matching` lines, one per name. The runner is correct, the doc is correct, and the missing summaries
were hidden by my own `tail -30`. All five files did run in the first invocation; my four per-file
re-runs were redundant confirmation, not a rescue. **The near-miss is the finding: I nearly edited a
correct doc to match a misreading produced by my own output truncation.** Same shape as the rejected
line-length criticism above — measure, then claim.

### Two convention criticisms I considered and REJECTED — recorded because they would have denied retirement

A clean solve here retires the task, so leniency and harshness both have a cost. Both candidate
defects were checked rather than judged by eye:

1. **"Its added doc lines run over 100 characters."** REJECTED — not a defect. `upgrade-catalogue.md`
   already carries 8 lines over 100 chars and `drivetrain-and-tires.md` carries 16, the longest being
   170 and 214. The probe matched local norms. **This is exactly the round-035 error** (criticising
   `rally_flag.gd` for raw `Color` constants when the file's own style used them), and the only reason
   I did not repeat it is that I measured the distribution instead of eyeballing it.
2. **"It deleted an inter-doc cross-reference."** REAL but below threshold. Rewriting the trade-off
   paragraph to cover three parts instead of two, it dropped the
   `[drivetrain-and-tires.md](drivetrain-and-tires.md) → Surface-specialised compounds` link. Two other
   links to that file survive in the same doc, and the replacement prose states the rule inline
   ("penalties elsewhere pay for bonuses on their home surfaces"), so the reader is not stranded.
   **Held as an observed blemish, not a convention violation** — a competent human condensing that
   paragraph would plausibly do the same. Flagging it explicitly so the user can overrule me: if this
   counts as convention 2, the round is 3/3/2/3, T001's counter resets to 0, and there is no retirement.

I also verified the probe **added** rather than moved or duplicated the tyre-slot note: the numbered
procedure occurs exactly once in both trees. My first diff read suggested a deletion, but it was taken
against `HEAD` and so folded in the loop's own uncommitted round-018/020/039 rewrites.

## Verdict on the pre-registered predictions

1. **"A clean solve, retiring T001 — held at ~50/50 and no higher."** **CONFIRMED**, at the odds I
   stated rather than better than them. Docs came in 2/2 and the round is 3/3/3/3. Calibration note:
   the coin-flip framing was right to be humble, but this is now the **third consecutive round in which
   the tyre slot's docs came in at 2/2** (021, 040 clean; the slot has not produced a 0/2 since round
   018's rewrite). The "irreducibly noisy" conclusion from round 032 was about docs ACROSS tasks; for
   THIS slot the evidence now points to genuinely fixed, not lucky.
2. **"Round 039's gate-id rule holds."** **CONFIRMED but only weakly tested.** The probe left the part
   ungated, which the note permits, and stated its reason — so the rule's *branch* was exercised, not
   its substance. No invented rally id appeared, but no real one was needed either. Round 039's fix
   remains essentially unmeasured; a future round wanting to test it needs a task that FORCES a gate.
3. **"The symmetric invariant holds."** **CONFIRMED**, and by a narrow margin (race_tires keeps snow by
   0.01), which is a stronger pass than a comfortable one — the probe priced the penalties precisely
   enough to win a surface without stealing one.

## The risk I recorded before dispatch, resolved

I predicted that round 039's ~9-line addition might have **diluted** a note that was working, and
committed in advance to shortening rather than adding if T001 regressed on ground the note already
covered. **It did not regress** — 3/3/3/3, docs 2/2, invariant held, on attempt 1. So the note absorbed
round 039's addition without measurable cost. This does not license further growth: the block is now
~45 lines and the ceiling §2.6 warns about has still not been located, only not yet hit.

## Attempt curve, and the cost of a first-attempt pass

One attempt, no fixes applied. §D2 is explicit that stopping early "is often correct and needs no
apology" — but the honest accounting is that **a round that passes on attempt 1 measures the codebase
and improves it not at all.** Nothing was fixed because nothing failed. The value delivered is the
retirement itself: a live task moved to `solved.md` on evidence, which is what §1 says to read instead
of the scores. Cost: 1 probe (~94k subagent tokens, ~125 s) plus five test files.

## Round state

- **No code, test, config or doc file was edited in the main checkout this round.** The closing-suite
  obligation (§2.7) is therefore satisfied by the five green files run against the probe's tree, which
  differed from main only by the probe's own diff. The green-tree invariant is unchanged from round 039.
- Worktree `drill-T001-r40` removed; `git worktree list` shows the main checkout only.
- Bank: **13 live tasks** (T003–T015), above §1's floor of 8. T002 `too_hard`, T001 **retired**.
- Test mode `fast`: full run still **OWED — now forty rounds**.

## Still owed to the user, unchanged and now more pressing

1. **A full `./run_tests.sh`** — forty rounds outstanding. Totals conservation remains UNVERIFIED.
   I will not start one unprompted (standing instruction: targeted runs only).
2. **A commit decision on rounds 015–040.** Everything since `e0b1a30` is uncommitted, which is now a
   large body of work — the renames in `save_manager.gd`, the notes, and ~10 new guard tests.
3. **The T003 rubric call** — whether inheriting Greece's `tree_mix` via the codebase's own `look_from`
   idiom satisfies "its own scatter set". Decides whether T003's 3/2/3/3 was in fact a clean solve, and
   T003 is now a candidate for the next retirement.

**Next round should** target a **second retirement**, since retirement is the loop's real output and
T001 just proved the mechanism works. **T004** is the pick: `clean_solves: 1` from round 023, one clean
solve from retiring, and untested since. The alternative — a task that FORCES a rally gate, to actually
measure round 039's fix, which prediction 2 shows is still essentially unmeasured — is the better
scientific choice if the user would rather validate a fix than bank a second retirement.

CONTINUE
