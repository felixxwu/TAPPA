# Round 024 — 2026-08-19 — drill mode

**Test mode: `fast`.** Totals UNVERIFIED; full run **OWED** (twenty-three rounds).
**`MAX_ATTEMPTS = 3`.** No compaction tool; scores to disk before diagnosis (§D3a).

## Sampling — sweep step 2

**Drawn: T009 — "Add a wet-weather tyre compound that grips better in the rain."**
areas: catalogue, upgrades, physics, weather. Unprobed since **round 010**; best **3/3/2/2**.

Its own bank entry calls it "the retirement candidate for the whole grip area" and "DELIBERATELY HARDER
than T001" — T001 has a legitimate no-new-axis answer, this one does not, because "grips better in the
rain" is a condition no existing axis expresses.

**Why this is the right next draw and not just the next id in the list.** T009 sits in the **same tyre
slot** as round 021's clean solve, so it is directly exposed to the note chain that produced it: the
numbered procedure (018), mechanism -> invariant (019), and the symmetric invariant (020). T001's clean-solve
probe updated all three of the `features/upgrade-catalogue.md` places that round 010 recorded as **stale
and unguarded** for T009 (the unlock-id list, the compound list, and the menu-label count). If that note
generalises, it should show here.

**One correction to my own framing from last round, stated so the reasoning is not misread:** I said T009
would benefit from "T001's brand-new gravel axis". That is wrong — a probe's diff is a *measurement* and
is never landed (§"Out of scope"), so `main` has **no** gravel axis and the tyre slot still holds only
snow and race. What T009 inherits is the **notes**, not the part.

## PREDICTIONS, written before dispatch

1. **The two tyre docs get updated** (`drivetrain-and-tires.md`, `upgrade-catalogue.md`) — the numbered
   procedure at the slot names both, and that has now worked twice (rounds 019, 021).
2. **`features/weather.md` does NOT get updated.** It is T009's third `expected_doc` and **nothing at the
   edit site names it** — the slot's procedure lists only the two tyre docs. This is the cleanest available
   test of whether the docs lever is really "the note names it" rather than "the probe reasons about
   relevance": a doc that is plainly relevant but unnamed should be missed.
3. **The axis work is done completely** — all three `game_config.gd` edits, reading `ctx["is_wet"]` (round
   009's bool) rather than comparing weather strings, so it fires in storm as well as rain.
4. **No test written** (round 022 settled this).

Prediction 2 is the interesting one: if the doc is updated anyway, the lever is broader than "naming", and
round 019's conclusion needs widening.

## Attempt 1

**Seed:** `.claude/worktrees/drill-T009-a1`. Rubric scrub clean; the numbered procedure, the symmetric
invariant and the `is_wet` bool all verified present.

**Probe diff — three files:**

- `scripts/game_config.gd` — **all three registry edits**: `@export var tire_wet_grip_mult := 1.0`, the
  `TIRE_SURFACE_AXES` row `{"field": "tire_wet_grip_mult", "channel": "wet"}`, and a `_channel_weight`
  arm reading **`ctx.get("is_wet", false)`** — round 009's bool, **not** a weather-string comparison, so
  it fires in **storm** as well as rain. That was the defect three consecutive rounds shipped.
- `scripts/upgrade_library.gd` — `wet_tires`, flat 1.12 + `tire_wet_grip_mult` 1.15.
- `features/drivetrain-and-tires.md` — the compound list, the effect-keys table row for the new axis, the
  trade-off prose, and the add-an-axis section.

**Tests I ran:** `test_tire_surface_axes` 6 · `test_upgrade_library` 46 · `test_upgrades_grid` 46 ·
`test_drivetrain` 33 — all green. **`test_weather` matched no file** (4 `Totals` for 5 names — the real
name is `test_weather_library`); recording that rather than letting the silence read as a pass.

### SCORES — 3 / 2 / 2 / 3

- **navigation 3.** Every site right: all three registry edits in one file, no consumer touched, the
  `is_wet` bool found, the right slot, the right doc.
- **correctness 2.** The grip mechanism — the task's actual subject — is **complete and correct**,
  including the storm case that defeated rounds 006, 007 and 009. **But the part is authored with
  `unlocked_by_rally: "h_coast_qualifier"`, and no such rally exists.** I checked every gate in
  `UPGRADES` against every `id` in `RALLIES`: main is clean, so this id is invented. The consequence is a
  **permanently unreachable part** — it can never be unlocked in normal play, so the feature ships dead.
  Round 002 treated exactly this shape (an inert region no rally tags) as a hard failure for T003.
  **Borderline 2 vs 1, and I am saying why rather than leaving it to look decisive:** I keep 1 for changes
  that are wrong or do not compile; this one is right-but-ungettable, which is nearer round 019/020's
  dominated-part 2s. It is a one-string fix, and the guard below makes it impossible to repeat.
- **convention 2.** One of three `expected_docs`. `features/upgrade-catalogue.md` (which round 010
  recorded as stale in three places for exactly this task) and `features/weather.md` both untouched.
  No test — expected, and settled in round 022.
- **completion 3.** Coherent and finished; its report worked the wet/dry arithmetic through.

**Against round 010's 3/3/2/2:** completion up, correctness down — and the correctness drop is the
invented gate, not the physics.

### Verdicts on the four predictions

| # | prediction | outcome |
|---|---|---|
| 1 | both tyre docs updated | **REFUTED** — 1 of 2 (`drivetrain-and-tires.md` only) |
| 2 | `features/weather.md` NOT updated | **CONFIRMED** |
| 3 | axis complete, reads `is_wet`, fires in storm | **CONFIRMED** |
| 4 | no test written | **CONFIRMED** |

**Prediction 1's refutation matters more than prediction 2's confirmation.** The numbered procedure names
both docs and sits in the file the probe edited — and it got one. Tallying every measurement of that lever:

| round | task | docs |
|---|---|---|
| 017 (pre-fix, both A/B arms) | T001 | 0/2 |
| 019 | T001 | **2/2** |
| 021 | T001 | **2/2** |
| 022 | T006 | 1/2 |
| 024 | T009 | 1/2 |

**So the honest characterisation is that the numbered procedure raises doc compliance from reliably-zero
to usually-partial-or-complete — it does not guarantee it.** I stated it too strongly in round 019 ("the
usable rule…"), and the two 1-of-2 results since are the correction. Notably the geometry is identical
between round 020's probe (2/2) and this one (1/2) — both inserted their row at line 190, immediately
before the note — so the difference is **variance, not position**, and no further position work is
warranted.

## TWO CORRECTIONS TO THE ABOVE, both from continuing to verify after I had written a score

### Correction 1 — the defect IS guarded. I ran too narrow a selection and nearly recorded the opposite.

I wrote "no test catches it", on the strength of a run covering `test_tire_surface_axes`,
`test_upgrade_library`, `test_upgrades_grid` and `test_drivetrain` — all green. **The guard exists and
lives in `test_rally_library.gd`, which I did not run.** Its comment states the exact failure mode in
advance:

> "The gates are plain id strings with no validation, so renaming or deleting a special would silently
> make the gated part permanently unwinnable (rally_gate_met would just return false forever, with no
> error). Pins no chosen id or threshold — it asserts the CONTRACT that every authored gate resolves."

Run against the probe's tree it fails four times, precisely:

```
wet_tires is gated on 'h_coast_qualifier', which must be a real rally
wet_tires is gated on 'h_coast_qualifier', which must be a SPECIAL event
```

**So there is no fix to make here — the repo was already protected, by a guard written for exactly this.**
Had I stopped at the first green run I would have "discovered" an unguarded defect and built a duplicate
guard for it. `CLAUDE.md`'s instruction to be **generous about the blast radius** is what I under-applied.

**Why I under-applied it, which is the transferable part:** I took the selection from T009's own
`expected_tests` (`tire_surface_axes`, `upgrade_library`, `upgrades_grid`), and that list omits
`rally_library`. **A rubric's `expected_tests` is a hint, not a blast radius** — the blast radius follows
from what the change touches (here: a new `UPGRADES` row with a gate field, which is rally-library
territory). Rubric fixed below so the next round does not inherit the same too-narrow run.

### Correction 2 — correctness 2 -> 1, for consistency with how I graded round 021

The change **reddens a pre-existing guard asserting a real contract**. That is the same situation as round
021's T006 probe (which reddened `test_brake_lockup` and scored correctness **1**), and I should not grade
the same shape differently three rounds apart because this one's physics impressed me more. The
mechanism being correct does not offset a red.

**Revised scores: 3 / 1 / 2 / 3.**

This makes the round a clearer regression against round 010's 3/3/2/2 — and the honest read is that T009
is **not** near retirement, contrary to its bank entry's hope, because the invented-gate failure is a
different defect from the three the previous rounds fixed.

## Fix — nothing in the codebase; one rubric bug that caused a mis-measurement

**No codebase fix this round, and that is the correct outcome:**

- the three causes rounds 006/007/009 fixed are all **settled** — the probe made every registry edit,
  touched no consumer, and read the `is_wet` bool instead of comparing weather strings, so the storm case
  that defeated three consecutive rounds is gone;
- the new failure (an invented gate id) is **already guarded**, by a test written for precisely it;
- inventing a second guard for a guarded defect would be duplicate machinery, and §2.6 warns against
  manufacturing a fix to fill the section.

**What I did fix is the rubric**, because it caused a real mis-measurement: T009's `expected_tests` omitted
`rally_library`, my selection followed it, and the first run came back green on a change that ships a
permanently unwinnable part. Added, with the reason recorded so the next round inherits the wider run and
the general lesson (**a rubric's `expected_tests` is a hint, never the blast radius**).

## Closing — §2.7 · §2.8 · §2.9 · §2.10

**No main-checkout code change** (only `tasks.md` and this report). Verification is the probe's own blast
radius: `test_tire_surface_axes` 6 · `test_upgrade_library` 46 · `test_upgrades_grid` 46 ·
`test_drivetrain` 33 green, `test_rally_library` **111 passing / 1 failing** — the red being the probe's
invented gate, which is the finding rather than a problem with main. Main is unchanged and was last
verified green in round 021.

288 authored config values (unchanged) · 544 uid sidecars (unchanged) · 0 duplicate `class_name`.
**Zero test-count delta.** `drill-T009-a1` removed; worktree list shows only the main checkout.

**`fast` costs:** Totals **UNVERIFIED**; full run **OWED — twenty-three rounds**.

## Round cost

One probe cycle (~108k subagent tokens, ~154 s) plus three parent test runs.

## Verdict

**CONTINUE.**

**1. T009 regressed to 3/1/2/3 on a NEW cause** — an invented `unlocked_by_rally` id that makes the part
permanently unwinnable. The three causes earlier rounds fixed are settled, including the storm case that
had recurred three rounds running; this is a different defect, so the task is **not** the near-retirement
candidate its bank entry hoped.

**2. I corrected myself twice, both times because I kept verifying after writing a score.** I nearly
recorded a guarded defect as unguarded (the guard lives in a test file the rubric's `expected_tests` does
not name), and I had scored a red-test regression as correctness 2 where round 021 scored the identical
shape 1. Both corrections are in the report with their reasoning; the second changed the round's headline
from "best-ever-ish" to "regression".

**3. The docs lever is weaker than I stated in round 019, and here is the full tally:** 0/2 before the fix
(both A/B arms), then 2/2, 2/2, 1/2, 1/2. It raises compliance from reliably-zero to
usually-partial-or-complete; it does not guarantee it. Round 020's probe and this one had **identical
insertion geometry** relative to the note (both at line 190) and scored 2/2 and 1/2 — so the residual is
**variance, not position**, and no further position work is warranted.

**Next round should:** finish the sweep with **T010** (countdown beep, unprobed since round 010, best
3/3/2/1 — a completion gap). After that the sweep is done and round 022's question ("how much longer?")
should be answered with the sweep's evidence in hand.

**Owed to the user:** a full `./run_tests.sh` (twenty-three rounds), a decision on committing rounds
015–021, the T003 rubric call, and the how-much-longer question.
