# Round 043 — 2026-08-19 — drill mode

**Test mode: `fast`, but a FULL RUN is in flight** (the user asked for it directly, which resolves the
forty-two-round debt). `MAX_ATTEMPTS = 3`. Scores to disk before diagnosis (§D3a).

## The round OPENED but deliberately did not dispatch yet

The `/loop` wakeup that opened this round arrived while the user's full `./run_tests.sh` was still
running. **I did not start the inner loop**, for the same reason a mid-round wakeup must not start a new
round: grading an attempt requires me to run its blast radius, and a concurrent run interleaves with the
full suite and makes **both** results untrustworthy. There is also a subtler hazard — seeding a probe
worktree copies `.godot`, and copying it while a live run is writing to it risks a torn cache in the
probe's tree, which would corrupt the measurement rather than merely delay it.

So this round's sampling is pre-registered here and dispatch waits on the suite. Bookkeeping that touches
no code and runs no tests is the only safe work in this window (§D2), and this is it.

## Sampling — T004, pre-registered

**Drawn: T004 — "Show the current gear on the HUD."** `clean_solves: **1**` (round 023's 3/3/3/3).

With **T001 reset to 0 by round 042's ratchet**, T004 is now the **only task in the bank within one clean
solve of a retirement** — and the loop currently has none. Untouched since round 023, which is twenty
rounds of accumulated change to its area.

**Its obligation set is the leanest in the bank** (round 023's note): a one-line `DEBUG_READOUT_NODES`
membership edit plus `features/hud.md`. Round 008 deliberately deleted the per-element membership tests
because they pinned a product choice, so the contract is now carried by two membership-driven tests that
iterate the constant — **a correct answer requires no test edit at all.**

## PREDICTIONS, before dispatch

1. **A clean solve — ~55%.** Higher than a typical draw because the obligation set is tiny and the docs
   half is the only historical failure (rounds 005/006/007 all shipped the code and skipped `hud.md`;
   round 007's fix named the docs AT the constant, and round 023 then went 2-for-2 including a doc not
   even in `expected_docs`). Tempered because docs remain the noisiest axis in the loop.
2. **No test file is edited — ~70%.** If a probe *does* rewrite a test here it is a defect, not diligence:
   round 008 removed those tests on purpose and the note says so.
3. **Round 042's freshly widened count guard stays green — ~90%.** T004 touches HUD docs, not catalogue
   prose, so it should not interact. Recording it because a guard widened two rounds ago has not yet been
   exercised by an unrelated task, and an over-broad guard would show up as a spurious red here.

## ROUND CLOSED WITHOUT DISPATCH — the user stopped the loop

No probe was dispatched, so **there is nothing to grade and no fix was made this round.** The T004 draw and
the three predictions above are pre-registered and untouched: whoever resumes the loop can dispatch
straight into them, and the predictions stand as written *before* any probe ran, which is the property that
makes them worth anything.

**State left clean:** no worktrees on disk, no probe in flight, no half-applied edit. The main checkout
carries rounds 015–042's work, uncommitted, exactly as round 042 closed it.

**Resume point:** seed `.claude/worktrees/drill-T004-r43` from the current checkout and dispatch one Haiku
probe with the bare task text plus the tests/Godot/commit bans. Do not re-draw — T004 is already the
correct draw for the reason recorded above, and re-drawing would discard a pre-registered prediction.

STOPPED (user request)
