# Round 036 — 2026-08-19 — drill mode

**Test mode: `fast`.** Totals UNVERIFIED; full run **OWED** (thirty-five rounds).
**`MAX_ATTEMPTS = 3`.** No compaction tool; scores to disk before diagnosis (§D3a).

## Sampling — acting on round 035's method change

**Drawn: T008 — "Give the player a star bonus for finishing a rally without any damage."**

Round 035 concluded that authoring new tasks costs two or three rounds of rubric debugging each, while probing
**well-characterised** tasks yields clean measurements. T008 is the strongest such candidate:

- **six rounds of accumulated, corrected rubric** (002, 005, 007, 008, 009, 010) naming five distinct traps that
  were each fixed structurally;
- **unmeasured since round 010** — twenty-six rounds of changes have landed since;
- and it is the task whose **frozen assertion round 016 removed**, so this is the first *independent* confirmation
  that the unfreeze holds. Round 016's own probe proved it once, immediately after the fix; a second probe twenty
  rounds later tests whether it stayed fixed.

**I applied round 035's mechanical rule before writing anything** — grep the request's key noun across `scripts/`
rather than trusting memory:

- `grep -rniE 'clean_run|no_damage|damage.*bonus'` finds **no existing bonus** — only `rally_session.gd:75`'s note
  ("Anything asking 'was this a clean run' must read this flag, not HP") and unrelated damage comments.
- `_award_any_finish_bonus_stars` still `return 0` — the seam is empty.
- `test_rally_session.gd` still carries **3 `assert_gte`** — round 016's unfreeze is in place.

So the feature genuinely does not exist, and nothing forbids it. **This is the check whose absence cost rounds 033,
034 and 035; it took one command.**

## PREDICTIONS, before dispatch

1. **No pre-existing test reddens.** Round 016 verified this once; twenty rounds of unrelated change have landed
   since, so this is a re-verification of the unfreeze rather than a new claim.
2. **It reads the latched `_took_damage_this_rally` flag, not end-of-rally HP** — `rally_session.gd:75` states the
   rule at the flag, and rounds 019/027/031 all showed a rule at the site being obeyed.
3. **It returns an int through `_award_any_finish_bonus_stars`** rather than inventing a result key — round 009 made
   an invented key structurally unexpressible.
4. **The amount lands in a `GameConfig` `@export`** rather than a literal — round 002's trap, and round 021's probe
   already did this correctly on T006.
5. **Docs recorded, not predicted.**

**What would be most informative:** a red test, because it would mean the round-016 unfreeze regressed or a *new*
frozen assertion appeared in twenty rounds of change — and that is exactly the class of rot the loop has now caught
three times (round 024's narrow `expected_tests`, round 027's non-recursive scan, round 034's missed accessor).

## Attempt 1 — SCORES: 3 / 3 / 2 / 3. All five predictions confirmed.

**Probe diff — three files:**

- `scripts/game_config.gd` — `@export_range(0, 30) var clean_run_bonus_stars := 1`, in the Star Economy group.
- `scripts/rally_session.gd` — three lines in the seam: `if not _took_damage_this_rally: return Config.data.clean_run_bonus_stars`, plus a rewrite of the seam's own comment.
- `features/reward-system.md` — the seams table updated from "*nothing yet — returns `0`; this is the empty seam*".

**Tests I ran:** `test_rally_session` **64** · `test_reward_system` 18 · `test_star_row` 9 · `test_menu_flow` 207 ·
`test_save_manager` 108 — **`ALL TESTS PASSED`**; project loads, 0 script errors.

| # | prediction | outcome |
|---|---|---|
| 1 | no pre-existing test reddens | **CONFIRMED** |
| 2 | reads the latched `_took_damage_this_rally`, not HP | **CONFIRMED** |
| 3 | returns an int through the seam, no invented result key | **CONFIRMED** |
| 4 | amount in a `GameConfig` `@export` | **CONFIRMED** — and correctly with **no** `.tres` line, since the default *is* the authored value |
| 5 | docs recorded, not predicted | 1 of 3 (`reward-system.md`; `star-economy.md` and `damage.md` untouched) |

- **navigation 3.** The seam, the right `@export` group, and the owning doc.
- **correctness 3.** Every trap this task accumulated over six rounds avoided: the **any-finish** gate rather than the
  podium block (round 005's trap), the **latched flag** rather than end-of-rally HP that pit repairs defeat (round
  002's), an **int** return rather than an invented key (rounds 008/009's), and a **tunable** rather than a literal
  (round 002's). I looked for a defect and found none.
- **convention 2.** Docs 1 of 3, and **no test** — which T008's own `test_conventions` asks for and round 010 called
  "legal and easy". It did rewrite the seam's comment, which its change falsified.
- **completion 3.**

**This equals T008's best-ever result (3/3/2/3, round 016) from an independent probe twenty rounds later** — a
reproducibility signal the loop rarely gets, since most tasks move between probes.

## THE ROUND'S FINDING: the unfreeze held, and its RATIONALE was read and used

Round 016 relaxed two `assert_eq` to `assert_gte` and left a comment at the seam explaining why. This probe's report
says:

> "Works seamlessly with existing tests (**which already use `>=` comparison instead of `==` for this exact
> reason**)"

and it **updated that comment's tense** rather than deleting it — "Tests that previously pinned
`stars_gained == stars_for_placement(...)` on undamaged fixtures now assert `>=`, because…".

Two things follow, and I want to bound the second carefully:

1. **The unfreeze is durable.** Twenty rounds of unrelated change did not reintroduce a freeze, and a fresh probe
   implementing the feature the freeze forbade sails through. Given that the loop has now found *three* past fixes
   that did not cover what they appeared to, a fix that verifiably still works is worth recording as such.
2. **A documented rationale was read and acted on — but for reassurance, not transfer.** The probe used the
   explanation to know it was *safe* to proceed. That is weaker than round 035's open question, which asks whether a
   rationale generalises to a decision it does not literally cover. This is the rationale being applied to **exactly
   the case it was written about**. Real evidence that such comments are read; **not** evidence that they generalise.

## Fix — nothing, and for once nothing was broken

- No red, no new cause, no defect in the probe's implementation.
- The seam's comment in `main` still says "RETURNS 0 TODAY, on purpose", which is **true of `main`** — the probe's
  diff is a measurement and is not landed, so there is nothing to correct.
- Docs 1 of 3 and the missing test are the harness-limited tail (rounds 022, 032); §2.6 forbids a fifth attempt at
  either.

## Closing — §2.7 · §2.8 · §2.9 · §2.10

No main-checkout change. Main unchanged since round 034's verified-green close; the probe's own blast radius is green
above. 288 authored config values · 544 uid sidecars · 0 duplicate `class_name` · zero test-count delta.
`drill-T008-r36` removed.
**`fast` costs:** Totals **UNVERIFIED**; full run **OWED — thirty-five rounds**.

## Round cost

One probe cycle (~80k subagent tokens, ~285 s) plus one parent test run — and no rubric debugging, which is the
point of round 035's method change.

## Verdict

**CONTINUE.**

**1. Round 035's method change paid off immediately.** Probing a well-characterised task produced a clean, fully
predicted measurement with **zero rounds spent on rubric repair** — against three consecutive rounds where new tasks
cost exactly that. The mechanical noun-grep took one command and confirmed the premise before any work.

**2. T008 equals its best-ever 3/3/2/3 from an independent probe twenty rounds after the first**, which is a
reproducibility datapoint the loop seldom gets.

**3. Round 016's unfreeze is verifiably durable, and its explanatory comment was read, cited and tense-corrected by
the probe.** Set against the three fixes since found not to cover what they claimed, a confirmed-still-working fix is
worth recording.

**4. Every remaining gap on T008 is the known tail** — two of three docs, and a test. Both are settled
harness-limited; there is nothing here left to fix.

**Next round should:** keep probing the corrected bank rather than extending it. **T003** is the standout —
3/2/3/3 in round 018, its only blocker a **rubric ambiguity awaiting the user**, and it has not been measured in
eighteen rounds. Failing that, **T005** (four distinct routes found, last probed round 017).

**Owed to the user:** a full `./run_tests.sh` (thirty-five rounds), a decision on committing rounds 015–021, and the
T003 rubric call — which now also gates the most attractive next probe.
