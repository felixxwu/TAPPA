# Round 045 — drill mode

- Date: 2026-08-19. Test mode: `fast`. MAX_ATTEMPTS: 3 (no drill-config.md override).
- Compaction: no tool exposed; append-as-you-go.
- Preconditions: P1/P2/P3 verified this session (round 044); baseline.md present. Worktree list clean.
- Size picture: unchanged from round 044 (scripts 205 files / 81,718 lines) — round 044's code delta was a comment block, one test file, two docs.
- **Sole-occupancy deviation, recorded:** a peer session worked in this checkout during round 044 and finished before this draw (TextField.wire_column FOCUS_NONE promotion; their uncommitted edits: scripts/text_field.gd, username_popup.gd, account_menu.gd, ui_theme.gd, tests/headless/test_text_field.gd, CLAUDE.md). They ran their own targeted tests green and released the lock. Probe worktrees seeded from now on will carry those edits; a red in menu-nav/account areas may be theirs — attribute before fixing. Draw deliberately avoids their area.

## Draw

**T014 — "On the overworld map, show how many stars I've earned on a rally when I look at its pin."**
Weighting: five tasks probed at most once; T014's area (overworld map, largest surface in the repo) not
probed since round 034, and clear of the peer's files. Prior score: round 034 = 3/3/2/3 (footer readout
correct, all four traps held in code; missed `features/map-exploration.md`, wrote no test, and wrote a
wrong prose rationale for the podium gate — fixed that round by a warning at `best_placement`).

## Pre-dispatch predictions (attempt 1, written before the probe runs)

1. **Traps 1–4 hold in code again — ~75%.** They held in round 034 and the seams have since gained the
   best_placement warning.
2. **The prose podium-gate error does NOT recur — ~70%.** Round 034's fix put the warning on the exact
   accessor a UI consumer reaches for; if a probe still writes "completed" for `> 0`, that warning has
   demonstrably failed twice and the next fix must be structural (rename or wrapper), not a longer note.
3. **Clean solve — ~40%.** The likeliest misses remain the second doc (`features/map-exploration.md`)
   and/or shipping no test for the new logic.

## Attempt 1

- Worktree drill-T014-a1 seeded from HEAD e277943 + uncommitted work (incl. peer edits, tested green by
  them). Seed + scrub checks green.
- Probe diff: **NONE.** The probe reported the feature "already fully implemented", citing
  `overworld_marker.gd::_apply_stars` and `overworld_zones.gd:344` — the 3D in-world pins, which DO show
  star rows. Verified: `overworld_map.gd` (the map screen the task targets) still references no star
  data (`grep best_placement|stars_for` → nothing). No test run needed (no diff).

**SCORES (written before diagnosis): navigation 1 / correctness 0 / convention 0 / completion 1.**

Prediction verdicts: 1–2 not exercised (no code written); 3 refuted (not clean, and not for either
predicted reason — a new failure mode for this task: FALSE ALREADY-DONE on an adjacent surface).

### Diagnosis (after scores)

Cause: **vocabulary/surface collision producing a false already-done.** Three surfaces render rally
pins — the map screen (`overworld_map.gd`, minimap + M-key full map), the in-world floating markers
(`overworld_marker.gd` / `overworld_zones.gd`), and the HQ map table (`hq.gd`). Two of the three carry
star readouts; the map screen does not. The request's nouns ("overworld", "pin", "stars") grep
straight into the marker implementation, whose star code is complete — so the probe verified the wrong
surface and reported "production-ready, no work needed" without ever establishing which surface the
request meant. Round 033 hit the same trap AT AUTHORING TIME (HQ table); this is its probe-side twin.
Nothing at any of the three sites says the others exist or that a readout on one is absent on another.

### Fix (attempt 1 → 2), in main checkout

1. `overworld_map.gd` header: a THREE-SURFACES disambiguation block — names the sibling surfaces and
   states (falsifiably, per the round-014 lever) that pin progress/star readouts exist on the markers
   and the HQ table but NOT on this map screen's pins.
2. `overworld_marker.gd` header: one cross-reference sentence (this is the in-world sign; the 2D map
   screen is `overworld_map.gd`, the HQ table is `hq.gd`; readouts here do not appear there).
3. `features/map-exploration.md` intro: the same three-surface fact, once.
Layers: authoring-time notes at both points of confusion + a falsifiable doc statement; no test can
catch "concluded done without a diff" (probes cannot run tests, and there is nothing red to show).
Durability: tier-4 notes + falsifiable doc sentence — accepted as the ceiling for this cause; if it
fails twice the next step is structural (e.g. renaming `overworld_map.gd` → `map_screen.gd` so the
noun "map screen" is the filename).
§D4 generalisation: serves T015 verbatim (same surface, same nouns) and any future map-screen task
("show rally names on the minimap", "colour pins by eligibility").

Prediction for attempt 2 (before dispatch): with the map screen's header stating pins carry no star
readout, the probe locates `overworld_map.gd` and ADDS the readout there (footer or asked-about pin)
instead of declaring done — expect navigation 3 / correctness ≥2 (~60%); clean ~35% (doc/test misses
remain likely). Refuted if it again reports already-done from the marker surface.

## Attempt 2

- Worktree drill-T014-a2 seeded with the disambiguation fix (seed + scrub checks green).
- Probe diff: **NONE** (worktree diff vs HEAD = my seeded patch only). It reported "already fully
  implemented" from `overworld_marker.gd`/`overworld_zones.gd` again — and this time it CITED my
  seeded `features/map-exploration.md` three-surfaces paragraph, describing it as documentation IT had
  updated. It read the disambiguation, including "the map screen's pins carry no progress/star
  readout", and still judged the in-world markers to satisfy the request.

**SCORES (written before diagnosis): navigation 1 / correctness 0 / convention 0 / completion 1.**

Prediction verdict: REFUTED, decisively — same conclusion, same surface, with the corrective fact in
its hands. Also noting the probe claimed authorship of a seeded doc edit (a faithfulness failure worth
tracking, separate from the axes).

### Diagnosis and DELIBERATE EARLY STOP at attempt 2 (per §D2)

Cause repeated identically across both attempts: false already-done from the marker surface. But
attempt 2 changes its interpretation. The probe read the corrective fact and still concluded done —
because **a plain-language reading of the task is genuinely satisfied by the game**: the overworld's
in-world pins DO show a star row when you look at them. The defect is the TASK WORDING, not codebase
legibility: "on the overworld map … when I look at its pin" cannot distinguish the map screen from the
in-world pins for a reader without the author's intent, and the round-033 retarget (HQ table → "overworld
map") fixed only one arm of a three-way ambiguity. What I considered and rejected for an attempt 3:
- A sharper note at the marker files keyed to the request's phrasing — rejected as task-shaped (§D4).
- Renaming `overworld_map.gd` → `map_screen.gd` — a real structural option, but it does not stop
  "overworld map" resolving to the in-world pins, which is where both probes concluded; it attacks the
  wrong arm of the ambiguity.
A third probe against an unchanged hypothesis would re-confirm, not inform.

**Consequences:**
- Attempt 1's three-surface disambiguation fix STAYS — it is true, general (§D4: serves T015 and any
  map-screen task), and the false-already-done it half-blocks is real. Its measured result is honest:
  one probe saw it and it did not flip the conclusion, so as the SOLE mechanism it is refuted; as
  orientation it costs nothing and remains correct.
- T014 is RE-AUTHORED (not too_hard, not discarded): wording now pins the surface the way a user
  actually would — by the action that opens it. New text: "When I open the full map with M, show how
  many stars I've earned on the rally pin I've highlighted." Rubric updated; clean_solves reset
  untouched (still 0). This is the round-033 authoring rule applied to its own residue: enumerate every
  surface the wording could mean; if two surfaces both satisfy it, the wording is the bug.
- The re-authored task is UNMEASURED this round (early stop) — the first thing a future round in this
  area should do is probe it.

## Close

- Inner loop: **T014, deliberate early stop at attempt 2 of 3** (two decisive attempts agreeing; the
  remaining defect is task wording, fixed by re-authoring, which no third probe on this round's
  hypothesis could measure). Attempt curve: 1/0/0/1 → 1/0/0/1 — flat, same cause both times, which per
  §D7 says the fix attacked the wrong layer; the layer it actually needed was the task bank.
- Per-attempt trace:
  - a1 → cause: vocabulary/surface collision → false already-done from in-world markers → fix:
    three-surface disambiguation notes (overworld_map.gd, overworld_marker.gd headers) + falsifiable
    statement in map-exploration.md → tier-4 note + doc, general (serves T015) → a2 outcome: refuted as
    a sole mechanism (probe read it and still concluded done).
  - a2 → cause: same, now attributed to task wording (the game genuinely satisfies a plain reading) →
    fix: T014 re-authored to name the surface by its opening action; attempt-1 notes kept as true
    orientation. Unmeasured.
- Faithfulness note: the a2 probe claimed my seeded map-exploration.md paragraph as its own doc update.
  Zero-diff "already done" reports need diff verification every time — both this round's probes wrote
  confident completion reports over empty diffs.
- Closing suite (mode `fast`): blast radius = header comments in two scripts + two eval/doc files →
  `--fast overworld_map` ALL TESTS PASSED; project loads headless. A full run is NOT owed (no
  behavioural code changed this round). Green-tree invariant remains `--fast`-deep.
- Totals conservation: UNVERIFIED (mode `fast`); no test files changed this round — delta zero by
  construction.
- Mechanical post-checks: config `=` count 307 (unchanged); no class_name duplicates; project loads.
  (uid check unchanged from round 044 — no file moves this round.)
- Teardown: worktrees a1, a2 removed; `git worktree list` shows main only.
- Tree at close: mine = evals/small-model/{tasks.md,rounds/044.md,rounds/045.md},
  features/{hud.md,debug-tools.md,map-exploration.md}, scripts/{hud.gd,overworld_map.gd,
  overworld_marker.gd}, tests/headless/test_hud_docs.gd. Peer session's (announced, theirs):
  CLAUDE.md, scripts/{text_field.gd,username_popup.gd,account_menu.gd,ui_theme.gd},
  tests/headless/test_text_field.gd.
- Cost: 2 probes (~75k + ~88k subagent tokens), 2 seeded worktrees, 1 targeted --fast run + 2
  project-load checks. Serial wall-clock ~20 min.

## Verdict

**STOPPED (user request) — "make this the last round."** This is NOT the §5 stop condition firing:
the loop was mid-signal (round 044 passed T004 at the cap; round 045 exposed a task-authoring bug and
a repeat false-already-done failure mode). Bank state at stop: 14 live tasks (T001, T003–T015 minus
T002 too_hard; T009 capped-not-discarded), floor satisfied; T014 re-authored and unmeasured; T004 at
clean_solves 1. Next-round pointers: probe re-authored T014 first; watch for the false-already-done
mode on other additive tasks; the T004 retirement needs one clean first-attempt solve.

