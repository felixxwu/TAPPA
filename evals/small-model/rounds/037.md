# Round 037 — 2026-08-19 — drill mode

**Test mode: `fast`.** Totals UNVERIFIED; full run **OWED** (thirty-six rounds).
**`MAX_ATTEMPTS = 3`.** No compaction tool; scores to disk before diagnosis (§D3a).

## Sampling — T005, the bank's most-fixed task, unmeasured for twenty rounds

**Drawn: T005 — "Track how many rallies the player has finished and show it on the profile."**

Round 036 showed that probing a corrected task yields a clean measurement with no rubric debugging, and recommended
T003 or T005. **T005 is the better choice: T003's only remaining blocker is a rubric call awaiting the user**, so a
re-probe there cannot resolve anything, whereas T005 carries **four distinct recorded failure routes and seven landed
fixes** — more than any other task.

**Noun grep first (round 035's rule), and the fix inventory verified rather than assumed:**

`grep -rniE 'rallies_finished|finished_count|record_rally_finish|total_rally_finishes' scripts/` returns **nothing** —
the counter is genuinely absent. And all seven fixes are present:

| round | fix | present |
|---|---|---|
| 014 | `test_every_persisted_key_written_is_declared_in_the_default_profile` | yes (×2 refs) |
| 015 | `rally_completed` -> `rally_podiumed` rename | yes |
| 015 | runtime undeclared-key tripwire | yes |
| 016 | `complete_rally` -> `record_podium_rally` rename | yes |
| 016 | write-site rule: "ANYTHING YOU INCREMENT OR WRITE IN HERE IS PODIUM-GATED" | yes |
| 016 | `test_the_podium_gated_recorder_writes_no_finish_named_profile_key` | yes |
| 017 | `test_no_local_variable_shadows_a_method_of_the_same_class` | yes |
| 034 | `best_placement()`'s "0 DOES NOT MEAN never finished" warning | yes |

## What this round actually tests: do four fixes hold against four known routes?

Every previous T005 probe failed a **different** way, and each got a fix:

| round | route taken | fix landed |
|---|---|---|
| 015 a1 | derived a count from the gated field `best_placed` | finish-name guard + (034) the `best_placement` warning |
| 015 a2 | new key, correct seam, **undeclared** in `_default_profile()` | 014's ratchet + 015's runtime tripwire |
| 016 | new key, declared, but incremented **inside the podium-gated recorder** | 016's recorder guard + write-site rule |
| 017 | correct shape, but a bare `rally_id` bound to a method -> **project would not compile** | 4 locals renamed + 017's shadow guard |

**This is the loop's best available test of accumulated durability** — not one fix, but four, against four routes a
probe has actually taken.

## PREDICTIONS, before dispatch

1. **None of the four routes recurs.** The strongest claim of the round.
2. **The key is declared in `_default_profile()`** — 015 a2's route, now covered by both a static ratchet and a
   runtime tripwire whose rule sits at the top of `save_manager.gd`.
3. **The increment goes on the any-finish gate**, not inside `record_podium_rally` — 016's route, now covered by the
   rename *and* the in-function rule.
4. **The project compiles** — 017's route, now covered by the shadow guard and the renamed locals.
5. **Docs recorded, not predicted.**

**The informative outcome is a fifth route.** Four probes, four different failures, is a pattern; if a fifth probe
finds a fifth way, that says something about the task's breadth rather than the fixes' quality — and §D5's "a
different cause each attempt = the task is too broad to be one task" would finally apply with five data points
instead of three.

## Attempt 1 — SCORES: 3 / 3 / 1 / 3. All four known routes avoided.

**Probe diff — three files:**

- `save_manager.gd` — `"rallies_finished": 0` **declared in `_default_profile()`** with the backfill comment, plus
  `record_finished_rally(rally_id)` (idempotent via a per-rally `finished` flag) and `finished_rally_count()`.
- `rally_session.gd` — `if finished: Save.record_finished_rally(String(_rally.get("id", "")))` in `_resolve_results`,
  on the **any-finish** gate.
- `account_menu.gd` — a career-stats line ("X rallies finished · Y cars owned").

**Tests:** `test_save_manager` **108** · `test_save_sandbox` 6 · `test_rally_session` 64 · `test_account_menu` 16 ·
`test_script_breadcrumbs` 7 · `test_menu_flow` 207 — **`ALL TESTS PASSED`**; project loads, **0 script errors**.

### The four routes, checked one by one

| route | how it failed before | this probe |
|---|---|---|
| 015 a1 — derived from the gated field | counted `best_placed > 0` | **avoided** — its own counter, no gated field read |
| 015 a2 — undeclared key | `profile["rallies_finished"]` never declared | **avoided** — declared in `_default_profile()`, with the backfill rationale |
| 016 — incremented in the podium recorder | inside `record_podium_rally` | **avoided** — a separate function, called from the `finished` gate |
| 017 — bare `rally_id` bound to a method | project would not compile | **avoided** — `String(_rally.get("id", ""))` inline; 0 script errors |

**All five predictions confirmed.**

### Scores

- **navigation 3.** The declaration site, the any-finish gate, and a profile display — three files, all correct.
- **correctness 3.** I hunted specifically for a defect and found none. Two things I checked rather than assumed:
  - **the minimal record.** `rallies.get(rally_id, {"finished": false})` creates a record lacking `completed` /
    `best_placed` / `best_combined_ms` for a rally finished but never podiumed. Every consumer reads those with
    `.get(key, default)`, so it is safe — but it is a genuinely different record shape from
    `record_podium_rally`'s, and worth having verified rather than waved through.
  - **idempotence and `save()`.** The counter increments once per rally and `save()` is inside the guard, so a replay
    neither double-counts nor writes.
  It counts **distinct rallies finished** rather than total finishes — a legitimate reading, and the same one round
  015's attempt 2 took.
- **convention 1.** **Docs 0 of 2** (`save-persistence.md` and `star-economy.md` both untouched) and no test.
- **completion 3.**

## The finding, and the over-claim I am not going to repeat

Four probes of T005 failed four different ways. **This one avoided all four**, with every accumulated fix in place.
That is real evidence the fixes work.

**But one clean probe is not reproducibility, and I know that from this exact task.** Round 015's attempt 3 also came
back 3/3/1/3 and I wrote that correctness was "solved" — then round 016's settlement probe scored **3/1/1/3** on the
*same tree* by a route nobody had anticipated. The correctness history is now **3, 1, 0, 3** across four probes.

So the honest claim is narrow: **the four known routes are closed against this probe.** It is not "T005 is solved",
and a fifth probe could still find a fifth route — which is precisely what happened last time I stated it more
strongly. Round 016's correction is the reason this paragraph exists.

**What has changed since round 015, though, is worth stating positively:** the failures are no longer *silent*. Three
of the four routes now have machine guards (the declared-key ratchet, the recorder guard, the shadow guard) plus a
runtime tripwire, so a probe taking any of them reddens a test or fails to boot rather than shipping a plausible
wrong number. That is the durable half of the work, independent of how any single probe scores.

## Fix — nothing. No defect, no new route.

Docs 0 of 2 and the missing test are the settled harness-limited tail (rounds 022, 032); §2.6 forbids another attempt
at either. Nothing in the probe's implementation needs correcting, and nothing in `main` was found wrong.

## Closing — §2.7 · §2.8 · §2.9 · §2.10

No main-checkout change. Main unchanged since round 034's verified-green close; the probe's blast radius is green
above. 288 authored config values · 544 uid sidecars · 0 duplicate `class_name` · zero test-count delta.
`drill-T005-r37` removed.
**`fast` costs:** Totals **UNVERIFIED**; full run **OWED — thirty-six rounds**.

## Round cost

One probe cycle (~92k subagent tokens, ~154 s) plus one parent test run, and again **no rubric debugging** — the
second round running that the method change has paid for itself.

## Verdict

**CONTINUE.**

**1. The bank's most-fixed task came back clean on every known route** — four routes, four fixes, none recurred, and
the project compiles where round 017's probe broke it outright.

**2. I am explicitly not calling T005 solved**, because I made that mistake on this task in round 015 and round 016
refuted it within one round. Correctness across four probes reads 3, 1, 0, 3. One clean probe closes the *known*
routes; it does not bound the unknown ones.

**3. The durable half is that three of the four routes are now machine-guarded**, so a future probe taking them fails
loudly rather than silently — which holds regardless of any individual score.

**4. Round 035's method change has now paid off twice in a row** (rounds 036 and 037): a corrected task, a noun grep,
a clean measurement, no rubric repair.

**Next round should:** continue with corrected tasks. **T009** (wet tyre) is the best remaining candidate — round
024's invented-gate-id failure was caught by an existing guard, and it has not been probed since; or **T010**
(countdown beep), whose AUDIO RULES rewrite from round 025 has never been measured.

**Owed to the user:** a full `./run_tests.sh` (thirty-six rounds), a decision on committing rounds 015–021, and the
T003 rubric call.
