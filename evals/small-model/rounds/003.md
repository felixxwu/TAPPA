# Round 003 — 2026-08-18

**Modified round (user directive via /loop): NO TEST EXECUTION AT ALL — no full
suite, no `--fast` runs.** §2.3 and §2.7 are skipped; graders score diffs
without recorded test results (correctness graded statically); the closing
green-tree invariant is explicitly NOT re-proven this round. The user owns
running tests.

## Measurements

| | |
|---|---|
| `scripts/` | 191 files, 77,600 lines |
| Largest | `test_menu_flow.gd` 5845, `hq.gd` 4706, `game_config.gd` 3829, `terrain_manager.gd` 2895 |
| Tree state on entry | clean (rounds 001–002 committed as `6b55238`) |
| Inherited baseline | 3255 tests / 159,957 asserts / 335.1 s, ALL PASSED (round 002 close) |

## Sampling

T002, T005, T007 (never probed) + T001, T003 (re-probe: round 002's fixes —
`**Tests:**` lines, grip-consumption guard, region-reachability guard —
targeted exactly these; this measures whether they reach a small model).

**Seal note:** the tree is fully committed, so worktrees branched from HEAD
contain `evals/small-model/tasks.md` (the rubrics) and `rounds/*.md`. Sealed by
deleting `evals/` and `todo/small-model-readiness.md` inside each worktree
before dispatch.

## Scores (as graded)

| Task | Nav | Corr | Conv | Compl | Note |
|---|---|---|---|---|---|
| T002 pause focus | 3 | 2 | 1 | 2 | Right seam, sound mechanism; but no features/menus.md update, no nav test (CLAUDE.md mandates both), hard-coded 4-button identity check, and test_pause_menu's "focuses Resume" assertion now passes only incidentally. |
| T003 canyon region | 2 | 0 | 1 | 1 | Idiomatic entry + doc edit, but 3 of 4 authored texture paths don't exist (no guard covers region sky/grass/gravel paths — NEW gap), no rally tags it (read-and-IGNORED the "TWO edits" note), no tests read or written, features/terrain.md stale. |
| T001 gravel tyres | 2 | 2 | 1 | 1 | Round-002 note WORKED for production: full closed-pair expansion (all readers, reset, carry list) — part is live. But the note's checklist ends at production, so test_drivetrain.gd's 6 direct 4-arg calls now fail to COMPILE (whole file lost), the "CLOSED PAIR" note now lies above three exports, docs/tests untouched, unlocked_by_rally omitted. |
| T005 rally count | 3 | 3 | 2 | 1 | Best code of the round: reused Save.completed_rally_count(), safe build/refresh ordering, no test broken. But zero docs, zero tests — and completed_rally_count() actually counts TOP-3 finishes, so "Rallies Completed" mislabels the metric (the name lies). |
| T007 speed blur | 2 | 1 | 1 | 1 | Row nav-wired like siblings, persists, applies at boot; but OFF→ON mid-run is permanently dead (_ready early-returns with _mat unset), setting's owner is a static on the menu class instead of an apply-module (fps_setting/camera_manager pattern), no docs, no tests. Grader verified probe did NOT run Godot. |

**0/5 clean. All clean_solves stay 0; nothing retires.**

## Failure causes (taxonomy)

1. **Docs/tests "same piece of work" convention unreached from the code (×5, dominant — REPEAT and REGRESSED vs round 002's 4/5 doc rate).** Zero probes wrote or updated a test; 4/5 skipped features/ docs. The rule lives in CLAUDE.md and the **Tests:** lines live in the docs — but a probe editing a SCRIPT gets no in-file pointer to either. The breadcrumb must be in the script being edited.
2. **Point-of-use checklist stopped short (×1, high cost — T001).** The closed-pair note enumerated production call sites only; the probe executed the list completely and stopped where it stopped. test_drivetrain.gd's 6 direct calls (compile-breaking) and the note's own text were not on the list.
3. **Nothing guards REGIONS asset-path existence (×1 — T003).** 3 of 4 invented texture paths dangle and every existing test stays green; the -greece/-snow naming convention made fabrication feel correct.
4. **Explicit note read-and-ignored (×1 — T003).** The "TWO edits" note alone did not force the rally tag.
5. **A name that lies about its metric (×1 — T005).** completed_rally_count() counts top-3 finishes; the UI shipped "Rallies Completed".
6. **Disabled-state treated as terminal / no boot-apply owner (×1 — T007).** Confirms backlog item 8.

## Fixes (this round, main checkout — NO tests executed per user directive)

- **Fix A (cause 1):** every script named by a features doc's **Source:** line gets a one-line header breadcrumb: its doc(s), its covering test(s), "update both in the same change".
- **Fix B (cause 2):** the game_config.gd closed-pair note gains the missing checklist entries (test_drivetrain.gd direct callers; "this note is part of the change") — generalised as: point-of-use checklists must name test callers and themselves.
- **Fix C (causes 3+4):** new guard test (written, NOT run): every resource path in RegionLibrary.REGIONS resolves; region_library.gd header gains "filenames must exist — list textures/, never invent by analogy" and a strengthened TWO-edits wording.

## What changed (main checkout)

88 paths modified/added, no commits:
- 82 `scripts/*.gd`: new `# Docs:` / `# Tests:` header breadcrumbs (Fix A; pure
  comments — project loads headless with zero errors afterwards).
- `scripts/game_config.gd`: closed-pair note extended to test callers + self-update
  instruction (Fix B).
- `scripts/region_library.gd`, `features/regions.md`: never-invent-filenames +
  do-both-edits-now notes (Fix C).
- `tests/headless/test_region_assets.gd`: +1 guard test
  `test_every_authored_region_resource_path_resolves` (+ helper). WRITTEN, NOT RUN
  (user directive).
- `features/README.md`: breadcrumb convention stated.
- `evals/small-model/{tasks.md,rounds/003.md}`, `todo/small-model-readiness.md`:
  loop bookkeeping; all five rubrics reconciled.

## Conservation / verification caveats (user-directed deviation)

NO tests were executed this round (user: "don't run any tests at all"). Therefore:
- The green-tree invariant is NOT re-proven; the next round must not assume a
  verified-green baseline from this round. Fix A is comment-only and the project
  loads clean, so parse-level risk is nil, but the new region guard test is unrun.
- Totals conservation is unmeasurable (+1 test expected: 3255 → 3256).

## Mechanical post-checks

- `config/game_config.tres`: 277 keys, diff empty.
- uid sidecars: 522 tracked; no duplicate class_name; `godot --headless --quit`
  loads with no errors.

## Teardown

All 5 probe worktrees removed; `git worktree list` shows only the main checkout.
Post-probe boundary check: main tree unchanged while probes ran.

## Cost

5 Haiku probes (~416k tokens), 5 Opus graders (~234k), 0 test runs.

## Close

**Verdict: CONTINUE.** 0/5 clean, nothing retires. Headline: round 002's fixes
demonstrably reach the probes (T001 executed the closed-pair checklist completely;
T003 read the TWO-edits note) but checklists were executed *exactly and only as
written* — so the fixes must enumerate the FULL blast radius including tests and
the note itself. The docs/tests-in-same-change convention regressed to 1/5 and is
now addressed at the point of use (breadcrumbs in 82 script headers) — round 004
should re-probe T002/T005/T007-adjacent areas to validate.
