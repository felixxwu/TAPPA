# Backlog — small deferred follow-ups

Small deferred follow-ups extracted from now-deleted DONE specs during the 2026-07-16 housekeeping sweep.

- **Target-time calibration** — the length/corner-mix → seconds formula and its `GameConfig` difficulty weights behind `LapTimeModel` are still a placeholder pending a calibration pass (the stage clock is only as honest as this formula).
- **Save web round-trip** (from `save-persistence.md`) — verifying the profile round-trip on an actual web export (IndexedDB flush) is the only remaining open item; see todo/web-save-persistence.md.
- **Stage countdown polish** (from `stage-start-and-end.md`) — the GO-flash duration is a `StageManager` const (`GO_FLASH_SECONDS`) rather than a config knob; the per-tick countdown beep / GO sting audio hooks are noted but unwired (no `Audio` autoload yet — see todo/audio.md).
- **HUD progress readout** (from `track-progress-and-reset.md`) — §6's optional progress label is unwired; `TrackProgress.progress_percent()` is exposed and ready but not surfaced in `hud.gd` / `main.tscn`.
- ~~**Fellable billboard trees**~~ — **LANDED.** `BillboardField` now has the same `knock_down` / `reset_fallen` pair as `TreeMeshField` (`billboard_field.gd` → `knock_down`, `reset_fallen`), so the perf-toggle tree path fells too. The only part still outstanding is the **fell SFX**, which belongs to todo/audio.md.
- **Roadside sign polish** (from `roadside-signs.md`) — only `{1,2,3,4,Square,Hairpin}` are signed (gentle 5s/6s stay unsigned); and both A-frame panel faces use the same texture, so a back-face arrow reads mirrored (give each panel its own approach-correct material if it ever looks wrong).
- **Spectator polish** (from `roadside-spectators.md`) — re-export `spectator.glb` from Blender for correct material colours; an optional articulated ragdoll if the single-capsule tumble reads too stiff. Crowd cheering audio ties into todo/audio.md.
- **Retire DistantTerrain** (from `terrain-lod.md`) — the terrain LOD ring now reaches the horizon and `DistantTerrain` is already disabled (`distant_terrain_enabled = false`), but the code (`scripts/distant_terrain.gd`, `world.gd` wiring, its `GameConfig` block) has not been deleted yet — deferred cleanup. The 32 m `CHUNK_M` partition also remains an optional measured follow-up.
- ~~**Bush low-poly mesh**~~ (from `distant-terrain-and-sky.md`) — **LANDED.** Bushes are no longer cutout billboards: `foliage.gd::bush_mesh` builds the low-poly GLB (`GROUNDCOVER_SCENE`) with `TREE_CANOPY_SHADER` (no `discard`) into a `TreeMeshField`. The only part still outstanding is raising the far render distance if §1 view-distance work pushes the horizon out.
- **Cloud-save follow-ups** (from `cloud-save.md`) — deliberately out of scope for the first cut: (a) **account deletion / data export** — Firebase Auth `accounts:delete` plus a Firestore delete, plus a "download my data" route, worth having before any wide release; (b) **using the new REST layer for other Firestore collections** (leaderboards, ghosts) — `RestClient` / `AuthService` were built to be reused for exactly that, but note that any *competitive* value needs a trusted server path, since the current rules only enforce document ownership, not plausibility; (c) the manual verification checklist in `features/cloud-save.md` › "Not yet verified" — none of it has been run.
- **Rival difficulty rebalance after the grip servo** (from `grip-servo-steering.md`) — the steering servo raised the player from ~80% to ~100% front-grip usage, closing real time to every rival, since `LapTimeModel` already assumes perfect grip usage. A rebalance pass on `LapTimeModel` inputs or the roster's target times is still owed; don't read "the player beats the field" as a bug.
- **Catalogue fields for richer rally classes** (from `one-map-four-corners.md`) — following the standing rule in `features/rally-roster.md` (when a rally wants to group cars by a property the catalogue doesn't record, ADD the property rather than approximating it with a proxy):
  - **`aspiration`** (NA / turbo / supercharged) on `EngineLibrary.ENGINES` — unlocks "naturally aspirated only" classes. Belongs on the engine, not the car, so an engine swap changes eligibility (same reasoning as `displacement_l`).
  - **`era` / `decade`** on `CarLibrary.CARS` — unlocks period events. A body property, so it belongs on the car.
  - **A genuine kei event isn't viable yet** — `car_type: "kei"` picks out a single car, so `hc_lakeside_kei` is authored as a Japanese national class instead. Viable once the roster carries more kei entries.
  - Each new restriction field needs its text surface kept in step: a branch in `RallyLibrary.ineligibility_reason` (why a car was rejected) is the one live reader. Use `./report_eligibility.sh` when authoring (2-3 eligible cars per rally, hard floor of 1).
