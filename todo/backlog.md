# Backlog — small deferred follow-ups

Small deferred follow-ups extracted from now-deleted DONE specs during the 2026-07-16 housekeeping sweep.

- **Rally target-time calibration** (from `rally-roster.md`) — the real length/corner-mix → seconds formula and its `GameConfig` difficulty weights are still a placeholder pending a calibration pass; `TargetTime.derive` / `LapTimeModel` use a stand-in formula. Also cosmetic: an opponent name pool for the leaderboard.
- **Reward curve values → GameConfig** (from `reward-system.md`) — the `tier_ceiling` shape, the `f(difficulty)` remap (default identity), and `repair_kit_drop_weight` are meant to move into `GameConfig` in the balance pass rather than staying as in-code curves.
- **`repair_kit_hp` GameConfig tunable** (from `upgrade-catalogue.md`) — the repair kit's heal amount is currently passed in by the caller; it should become a `GameConfig` field. (The full authored part list per slot/tier is content/balance, not tracked here.)
- **Save web round-trip** (from `save-persistence.md`) — verifying the profile round-trip on an actual web export (IndexedDB flush) is the only remaining open item; see todo/web-save-persistence.md.
- **Stage countdown polish** (from `stage-start-and-end.md`) — the GO-flash duration is a `StageManager` const (`GO_FLASH_SECONDS`) rather than a config knob; the per-tick countdown beep / GO sting audio hooks are noted but unwired (no `Audio` autoload yet — see todo/audio.md).
- **HUD progress readout** (from `track-progress-and-reset.md`) — §6's optional progress label is unwired; `TrackProgress.progress_percent()` is exposed and ready but not surfaced in `hud.gd` / `main.tscn`.
- **Fellable billboard trees** (from `trees-falling-on-impact.md`) — `knock_down` felling shipped for `TreeMeshField` only; `BillboardField` (the perf-toggle tree path) shares `ObstacleBody` and could get the same one-MultiMesh-transform tilt but was left out of phase 1. Fell SFX ties into todo/audio.md.
- **Roadside sign polish** (from `roadside-signs.md`) — only `{1,2,3,4,Square,Hairpin}` are signed (gentle 5s/6s stay unsigned); and both A-frame panel faces use the same texture, so a back-face arrow reads mirrored (give each panel its own approach-correct material if it ever looks wrong).
- **Spectator polish** (from `roadside-spectators.md`) — re-export `spectator.glb` from Blender for correct material colours; an optional articulated ragdoll if the single-capsule tumble reads too stiff. Crowd cheering audio ties into todo/audio.md.
- **Diegetic HQ later slices** (from `diegetic-hq.md`) — the tuning-lift UI + inventory, the 3D reward-reveal rig + podium, camera fly-throughs between locations, designed environment art, and per-car paint / duplicate-model name suffixes ("MX-5 #2") all remain; see todo/menus.md (the umbrella spec they belong to).
- **Retire DistantTerrain** (from `terrain-lod.md`) — the terrain LOD ring now reaches the horizon and `DistantTerrain` is already disabled (`distant_terrain_enabled = false`), but the code (`scripts/distant_terrain.gd`, `world.gd` wiring, its `GameConfig` block) has not been deleted yet — deferred cleanup. The 32 m `CHUNK_M` partition also remains an optional measured follow-up.
- **Bush low-poly mesh** (from `distant-terrain-and-sky.md`) — the billboard→opaque-model swap + auto-LOD landed for trees only; bushes are still cutout billboards and need their own low-poly mesh, plus raising the far render distance if §1 view-distance work pushes the horizon out.
- Detune-to-enter flow is duplicated across hq.gd and start_line.gd (see git history of the deleted todo/housekeeping-cleanup.md) — left un-consolidated because the two flows differ materially; revisit only if they converge.
